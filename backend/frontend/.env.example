# Only used by the Vite dev server (vite.config.ts) to know where to proxy
# /api requests during local development. Not needed in production, where
# the built frontend is served behind the same reverse proxy as the API
# (see ../DEPLOYMENT.md).
VITE_BACKEND_URL=http://localhost:8000
